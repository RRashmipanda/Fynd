python -m venv myvenv
myvenv\Scripts\activate
pip install fastapi uvicorn motor passlib[bcrypt] python-jose pydantic-settings python-dotenv

Freeze packages (good practice)
pip freeze > requirements.txt

